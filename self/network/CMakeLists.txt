CMAKE_MINIMUM_REQUIRED(VERSION 3.15)
PROJECT(NETWORK)
SET(CMAKE_BUILD_TYPE Debug)

ADD_EXECUTABLE(__network network.c network.h)
SET_TARGET_PROPERTIES(__network PROPERTIES RUNTIME_OUTPUT_DIRECTORY ${CMAKE_SOURCE_DIR})